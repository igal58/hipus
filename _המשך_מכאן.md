# Hipis — מסמך המשך (Handoff)

> מחר / במחשב אחר: פתח את Claude בתיקייה הזו ואמור לו **"תקרא את _המשך_מכאן.md"** — והוא יהיה מעודכן לגמרי.
> עדכון אחרון: 2026-06-30 (ערב), commit `50e8ac5`.
> **המשך למחר — תקציר היום (2026-06-30):** סשן ארוך, 12 commits. נוספו: ייבוא מקישור Google Flights, חיפוש ברדיוס 1500 ק"מ סביב היעד, כפתור מלונות + עוד אתרי הזמנה לכל כרטיס, נפילה לכיוון-אחד, תווית הלוך-ושוב, ותיקון קישורי Google. ראה סעיף **"סשן 2026-06-30 — מה נעשה ומה פתוח"** למטה.
> **לפני כן (2026-06-25):** ערי עצירה (הלוך/חזור) על כל כרטיס · עד 12 תוצאות · טמפ׳ ביעד בשעת הנחיתה (°C/°F, תחזית/ממוצע) · כפתור מדריך "?" בנאבבר (3 שפות).
> **לפני כן: פייסליפט ברמת מוצר** — מותג **FlyFinder**, נאבבר אמיתי, מגירת "התאמה אישית", מפת מסלול טיסה, בורר מטבע/עצירות/מחלקה, מחיר שמתחשב בנוסעים, ושמירת מצב ב-F5.

## מה זה
אתר חיפוש כרטיסי טיסה (**קובץ HTML יחיד + שרת Node קטן — אין framework, אין build, אפס תלויות npm**).
עברית/אנגלית/רוסית, RTL מלא. מותג: **FlyFinder**.
**מטרת המשתמש:** להיות טוב יותר מ-Google Flights בנישה (ישראל, רב-לשוני, השוואת אתרים, מבצעים, עמלות), עם תחושת מוצר פרימיום.
**ספריות חיצוניות (CDN):** Font Awesome (אייקונים), flag-icons (דגלי SVG), Leaflet+OpenStreetMap (מפה), Inter/Heebo (פונטים). אין אמוג'י ב-UI.

## איפה זה חי
- **ענן (חי):** https://hipus.onrender.com  (Render, שכבה חינמית — נרדם אחרי ~15 דק׳, מתעורר ~50ש)
- **קוד ב-GitHub:** https://github.com/igal58/hipus  (פרטי, ענף `main`; Render בונה אוטומטית בכל push)
- **מקומית:** התיקייה הזו. הרצה: לחיצה כפולה על `הפעל.bat` (צריך Node מותקן).

## קבצים עיקריים
- `index.html` — כל ה-UI + הלוגיקה (HTML+CSS+JS, ~1950 שורות). כולל מערך `COUNTRIES` (233 ערים) ומפה `AIRLINE_NAMES`.
- `.github/workflows/price-alerts.yml` — GitHub Action שמפעיל בדיקת התראות כל 3 שעות.
- `server.js` — שרת מאוחד: מגיש את הדף + API מאותו origin (בלי CORS).
- `package.json` — להרצה בענן (`npm start`).
- `proxy.config.json` — **סודי, לא ב-GitHub**: מכיל את ה-API token + marker. חובה להעתיק ידנית למחשב חדש!
- `הפעל.bat` — מפעיל מקומי. `proxy.js`/`start_proxy.bat` — ישנים, לא בשימוש.
- `Backup/` — גיבויים עם חותמת זמן (גיבוי לפני כל שינוי).
- `.claude/launch.json` — הגדרת preview: `node server.js` על פורט 8787 (תוקן; קודם הצביע ל-Python בטעות).

## API של השרת (כולם מאותו origin)
> **פרמטרים משותפים חדשים:** `cur` (ils/usd/eur — ברירת מחדל ils) נתמך ב-offers/price/deals/calendar/flexmonths.
> `stops` (0–9, מקס׳ עצירות) נתמך ב-offers/price. השרת מסנן `transfers<=stops`.
- `GET /health` → `{ok, tokenConfigured, cloud}`.
- `GET /price?origin=&destination=&depart=YYYY-MM-DD[&return=YYYY-MM-DD]` → הטיסה **הזולה ביותר** (טיסה אחת). אם `return` ריק → חיפוש **דו-כיווני גמיש** (יציאה קבועה, חזרה פתוחה). מחזיר גם `departISO/returnISO/flex/departTime/returnTime`.
- `GET /offers?origin=&destination=&depart=[&return=]` → **עד 6 טיסות אמיתיות שונות** (מחיר/חברה/שעות/עצירות מגוונים), כל אחת עם קישור רכישה (marker). זה מה שהדף משתמש בו לתוצאות.
- `GET /deals?origin=TLV` → הזול מתל אביב לכל העולם (מבצעים חמים).
- `GET /calendar?origin=&destination=&month=YYYY-MM` → **מחיר זול לכל יום בחודש** (Travelpayouts `grouped_prices`, `group_by=departure_at`). מחזיר `{days:[{date,price,airline,transfers,link}], cheapest}`.
- `POST /alerts` (JSON: `email,origin,destination,depart,ret?,target`) → יוצר התראת מחיר. `GET /alerts?email=` → רשימת ההתראות של המייל. `GET /alerts/delete?id=&email=` → מחיקה.
- `GET /check-alerts?key=CRON_KEY` → בודק את כל ההתראות מול מחיר חי ושולח מייל למי שירד ליעד (one-shot). מופעל ע"י GitHub Action / cron חיצוני. מוגן ב-`CRON_KEY` אם מוגדר.
- `GET /flexmonths?origin=&destination=&from=YYYY-MM-DD&months=4` → **הזול ביותר בכל אחד מ-N החודשים הקרובים** (חיפוש גמיש). מחזיר `{months:[{month,date,price,airline,link}], cheapest}`.
- `GET /parse-gflights?url=<קישור Google Flights/Explore מקודד>` → מפענח את הפרמטר `tfs` (ולעיתים `tfu`) — protobuf ב-base64url שבתוכו תאריכים (`YYYY-MM-DD`) ומזהי Google Knowledge Graph (`/m/...`) — וממיר כל מזהה ל-IATA דרך **Wikidata**. מחזיר `{ok, depart, ret, dates, origin:{label,iatas[]}, destination:{label,iatas[]}, warnings[]}`. מזהה שלא נפתר → `warning` (לא ממציאים).

## מפתחות / חשבונות
- **Travelpayouts** (מקור המחירים): token שמור ב-`proxy.config.json` (מקומי) וב-Render כ-`TP_TOKEN`. marker = `742171` (אפיליאט). ⚠️ ה-token 32 תווים, מתחיל ב-`4a34`.
- בענן ה-token/marker מוגדרים כ-Environment Variables ב-Render (לא בקוד).

## הקמת התראות מחיר (כדי שיעבדו בענן — עדיין לא הוקם)
ה-MVP בנוי ועובד מקומית ב-dry-run. כדי שישלח מיילים אמיתיים בענן צריך 3 דברים:
1. **מפתח Resend** (חינמי, resend.com): צור API key, והגדר ב-Render env: `RESEND_API_KEY=...`. אופציונלי `ALERT_FROM="Hipus <דומיין-מאומת>"` (ברירת מחדל onboarding@resend.dev עובד לבדיקות).
2. **CRON_KEY** (סוד כלשהו) ב-Render env — מגן על `/check-alerts`.
3. **תזמון אוטומטי** — כבר מובנה ע"י `.github/workflows/price-alerts.yml` (רץ כל 3 שעות, מעיר את Render + מפעיל בדיקה). רק צריך: ב-GitHub → Settings → Secrets → Actions → להוסיף `CRON_KEY` (זהה ל-env ב-Render). חלופה: cron-job.org ל-`/check-alerts?key=<CRON_KEY>`. בנוסף יש מתזמן פנימי בשרת (`CHECK_HOURS`, ברירת מחדל 6ש) שרץ כשהשרת ער.
⚠️ **אחסון**: כרגע `alerts.json` (gitignored, מכיל מיילים). ב-Render חינמי הקובץ **ארעי** — נמחק בכל deploy/קור-סטארט. לפרודקשן אמיתי: לעבור ל-Upstash Redis (REST, חינמי) או DB. השרת מוכן להחלפת `loadAlerts/saveAlerts` בלבד.

## מה כבר עובד (הושלם)
1. **קישורים חיים** ל-Google/Skyscanner/Kiwi/Kayak/Momondo + **El Al** — בשורת "השוואה וחיפוש באתרים אחרים" (כפתורים **בלי מחיר**).
2. **~233 ערים** ב-3 שפות; חיפוש חוצה-שפות + IATA.
3. **תוצאות = כמה טיסות אמיתיות שונות** (`/offers`, עד 6) — מחירים/שעות/חברות מגוונים, לא אותו מחיר לכולם. כל כרטיס עם כפתור "רכישה ב-Aviasales" (עמלה). הזול מסומן תווית **"משתלם"** (לא "הזול ביותר", כי מקור אחד).
4. **שעות יציאה/חזרה** + **"ללא כבודה רשומה"** על כל כרטיס ובסיכום (אייקוני Font Awesome, בלי אמוג'י).
5. **ימי שהייה = 0 → דו-כיווני גמיש**: לא ממלא תאריך חזרה, נשאר 0; מחפש את הזול מהתאריך והלאה; מסומן "תאריכים גמישים"; במצב זה **לא מציגים זמן חזרה**.
6. **בורר נוסעים** (מבוגרים/ילדים/תינוקות) + **בורר כבודה** — באותה שורה עם כפתור החיפוש.
7. **מבצעים חמים** (`/deals`).
8. **תיקוני IATA**: KIV→**RMO** (מולדובה), ULN→**UBN** (אולן באטור), FRU→**BSZ** (בישקק/קירגיזסטן — Travelpayouts לא מכיר FRU), PNH→**KTI** (פנום פן/קמבודיה — נמל Techo החדש; PNH הישן "not flightable"). **ביקורת אמיתית** של כל 233 הקודים בוצעה ע"י קריאה בפועל ל-`prices_for_dates` (לא רק autocomplete!) — רק FRU ו-PNH היו שבורים, שניהם תוקנו. כל ה-231 הנותרים תקינים.
9. **שמות חברות תעופה**: מפה `AIRLINE_NAMES` (55 חברות, 3 שפות) ממירה קוד IATA (LY/EY/W6…) לשם מלא ("אל על"/"אתיחאד"). מוצג בכרטיס ובסיכום. קוד לא מוכר → מוצג כפי שהוא (בלי להמציא).
10. **"לאן שבא לי" לפי תקציב**: שדה תקציב (₪) ליד כפתור המבצעים → מסנן את `/deals` לפי הסכום, כותרת "לאן שבא לי — עד X ₪", הודעה ייעודית כשאין יעדים. ריק = כל המבצעים.
11. **לוח מחירים / החודש הזול**: כפתור "📅 החודש הזול" → רשת ימים עם מחיר זול לכל יום (לפי היעד הנבחר + חודש תאריך היציאה), הזול מודגש "הכי זול", לחיצה על יום ממלאת תאריך ומריצה חיפוש. משתמש ב-`/calendar`.
12. **קישור אל על תלוי-שפה**: `heb/israel` בעברית, `eng/israel` באנגלית/רוסית (rus/israel מחזיר 492). אין deep-link ציבורי יציב עם מסלול/תאריכים — לא ממציאים פרמטרים. אם המשתמש יספק URL אמיתי מחיפוש באתר אל על → אפשר לבנות deep-link מדויק.
13. **התראות מחיר (MVP, מייל)**: טופס "🔔 התראת מחיר" אחרי כל חיפוש (מייל + מחיר יעד למסלול הנוכחי). שרת שומר ב-`alerts.json`, שולח מייל דרך **Resend**. `/check-alerts` בודק מול מחיר חי ושולח כשירד ליעד (one-shot). בלי `RESEND_API_KEY` → מצב **dry-run** (לא שולח, רק מדווח — לבדיקות). מייל אחרון נשמר ב-localStorage. ראה "הקמת התראות" למטה.
14. **ימי שהייה=0 — הסבר + חלון רב-חודשי**: הערה תחת השדה מסבירה "0 = חיפוש גמיש מתאריך היציאה והלאה" (מודגשת כשהערך 0). בנוסף, חיפוש גמיש מציג **חלונית "כמה חודשים קדימה"** (`/flexmonths`) — הזול בכל אחד מ-4 החודשים הקרובים, הזול הכולל מסומן, לחיצה על חודש מחפשת לתאריך הזול שלו.
15. **התראות אוטומטיות בלי מסך**: (א) מתזמן פנימי בשרת (`setInterval`, ברירת מחדל כל 6ש, `CHECK_HOURS`) שרץ כשהשרת ער + self-ping (`RENDER_EXTERNAL_URL`) שמצמצם שינה. (ב) **GitHub Action** `.github/workflows/price-alerts.yml` שרץ כל 3 שעות, מעיר את Render וקורא ל-`/check-alerts` — עובד גם בלי מסך פתוח. צריך להגדיר secret `CRON_KEY` ב-GitHub (זהה ל-env ב-Render).
16. **פייסליפט — נאבבר + מותג**: נאבבר sticky עם מותג **FlyFinder** (סמל מטוס SVG + שם + כתובית), פעולות ניווט (חיפוש/מפה/מבצעים/החודש הזול/התראה — גוללות/מפעילות פונקציות קיימות), בורר שפה, וכפתור התאמה אישית. במובייל: **סרגל ניווט תחתון**. הכל מתורגם 3 שפות, RTL. ה-`<header>` והכפתורים המרחפים הישנים (נקודות צבע, toggle בהיר/כהה) **הוסרו**.
17. **מגירת "התאמה אישית"** (`#custDrawer`, toggle מהנאבבר): צבע מותג (6: blue/teal/purple/rose/orange/neutral), רקע (light/dim/dark), גודל טקסט (small/medium/large). חל דרך CSS variables (`applyCustomize()`), נשמר ב-localStorage, נסגר ב-X/רקע/Esc/כפתור.
18. **בורר מטבע ₪/$/€** (`currentCur`): בשורת כותרת התוצאות. עובר כ-`cur` לכל ה-API; הסמל (`curSym()`) מוצג בכל מקום. החלפה מרעננת מיד.
19. **מקס׳ עצירות (0–9)** + **בורר מחלקה** (Economy/Comfort/Business/First) + **נוסעים** — שלושתם בשורת פילטרים אחת (`.filters-row`). שינוי כל אחד → חישוב מיידי.
20. **מחיר שמתחשב בפרמטרים**: המחיר המוצג = מחיר-לנוסע × `paxCount()` × `classMult()`. עצירות מסננות אמיתית (שרת). **מחלקה** מסומנת `≈` ו"מחיר משוער" (Travelpayouts מחזיר רק תיירות → מכפילים: comfort×1.6, business×3, first×5; הקישורים לאתרי ההזמנה נפתחים במחלקה האמיתית). הכרטיס מציג סה"כ + פירוק "N × לנוסע".
21. **מפת מסלול טיסה** (`focusMap`): סמן מוצא + סמן יעד + **קשת מקווקוות מעוקלת** (`arcPoints` — בזייה ריבועית) + **אייקון מטוס** מסובב לכיוון (`bearing`), `fitBounds` לשתי הנקודות, מתעדכן בשינוי מוצא/יעד. קואורדינטות מ-autocomplete של Travelpayouts (cache לפי IATA).
22. **שמירת מצב ב-F5** (`saveState`/`restoreState`, localStorage `hipusState`): מוצא, יעד, תאריכים, ימי שהייה, מקס׳ עצירות, נוסעים, כבודה, שפה, מטבע, מחלקה, accent, רקע, גודל טקסט.
23. **שורת "החודשים הזולים ביותר"** בתחתית כל חיפוש (`fetchCheapestMonths` → `/flexmonths`, ממוין לפי מחיר, 4 הזולים). **תיקון חשוב:** calendar/flexmonths עברו ל-`one_way=false` (הלוך-חזור) כדי שיתאימו לחיפוש הראשי (קודם הציגו כיוון-אחד → פער מחירים מבלבל).
24. **שמות מותג/כבודה**: "EL AL" באנגלית (לא "El Al"); בעברית כבודה: "תיק טרולי" (יד) / "מזוודה" (רשומה).
25. **ערי עצירה לכל כרטיס (הלוך/חזור)**: ה-API לא מחזיר עיר עצירה כשדה — אבל המסלול מקודד ב-`link` (פרמטר `t=`, למשל `TLVFCOMUC`=TLV→רומא→MUC). השרת (`parseRoute`+`stopsFromRoute`) מחלץ את קודי השדות, **מאמת מול נקודות הקצה ומספר העצירות** (`transfers`/`return_transfers`), ומחזיר `stopsTo`/`stopsBack` (מערך ריק=ישיר, `null`=לא אומת — לא מוצג). הלקוח מתרגם כל IATA ל"עיר, ארץ" (`placeName`/`resolvePlace`: קודם `COUNTRIES` מתורגם, אחרת autocomplete, אחרת הקוד), עם **תוויות "הלוך"/"חזור"** מפורשות. ⚠️ התווית הצהובה ("עצירה 1") עדיין סופרת הלוך בלבד — מכוון.
26. **עד 12 תוצאות** (`fetchOffers`): היה 6 → הועלה ל-12 (מושך limit 60 ומסנן).
27. **טמפ׳ ביעד בשעת הנחיתה** (°C + °F): מקור **Open-Meteo** (חינמי, ללא מפתח, CORS). רגע הנחיתה = `departAt`+`durationTo` (נוספו ב-mapFare). עד ~15 יום → **תחזית שעתית אמיתית** (`/v1/forecast`); רחוק יותר → **ממוצע מאותו תאריך בשנה שעברה** (`archive-api`), מסומן `≈` + "ממוצע עונתי". אין נתון → לא מוצג. פונקציות: `fetchTemp`/`pickHour`/`arrivalAbs`/`landTempHtml`, cache `_wxCache` לפי שעה. נטען מראש ב-`fetchOffers` לפני הרינדור. החישוב כולו בצד הלקוח.
28. **כפתור מדריך "?" בנאבבר** (`#helpBtn` → `#helpModal`): מודאל שמסביר כל חלק באתר (חיפוש, כרטיס, עצירות, מזג אוויר, מטבע, מפה, החודש הזול, מבצעים/תקציב, התראה, התאמה אישית/שפה, שמירת מצב). התוכן נבנה מאובייקט `T[lang].help` (title/intro/secs) ב-`renderHelp()` → **מתחלף חי עם השפה** (עברית/אנגלית/רוסית). נסגר ב-X/רקע/Esc.
29. **ייבוא מקישור Google Flights / Explore**: תיבת הדבקה + כפתור "ייבא וחפש" בכרטיס החיפוש (`#gfUrl`/`#gfBtn`, 3 שפות). מדביקים קישור Google Flights → `importGFlights()` קורא ל-`/parse-gflights`, ממלא מוצא/יעד/תאריכים/לילות (`pickCountryByIatas`→`findByIata`) ומריץ `doSearch()`. **שרת:** `parseGFlights` מחלץ מיקומים מדויק לפי בתי protobuf (`extractLocations` — תג `0x12`+אורך, לא regex; קודם תיקון: ה-regex בלע את הבית הבא), ו-`resolveMids` מתרגם דרך Wikidata SPARQL (`P646`→`P238`, או שדה-תעופה שמשרת את העיר ב-`P931`, מסונן ל-`Q1248784`=airport; דורש User-Agent → `fetchJSONUA`). בחירה: מוצא=מיקום ראשון, יעד=המיקום הספציפי הבא **שמדלג על המוצא** ומעדיף עיר עם שדה-תעופה אמיתי (כך URL של Explore עם מדינה כללית + עיר ממוקדת → העיר הנכונה, למשל TLV→BOM). מזהה/עיר שלא במאגר → הודעה למשתמש, בלי המצאה.

## מפת ניווט בקוד (שמות מפתח ב-index.html)
- **מצב גלובלי:** `currentLang, currentCur, currentClass, currentAccent, currentBg, currentFont, selectedToCode, pax, bags, lastSearch`.
- **תרגומים:** אובייקט `T` (he/en/ru) — כל טקסט ב-UI עובר דרכו; `applyLang(lang)` מחיל הכל.
- **התאמה אישית:** `ACCENTS`, `FONTS`, `applyCustomize()`, מגירה `#custDrawer` + `toggleCustomize/openCustomize/closeCustomize`.
- **חיפוש:** `doSearch()` → אם ימי-שהייה=0 → `resolveFlexDates()` (גמיש), אחרת `render()`. `render()` בונה סיכום+קישורים+טופס התראה ומריץ `fetchOffers()`+`fetchCheapestMonths()`.
- **כרטיס תוצאה:** `offerCard()`. מחיר: `paxCount()`×`classMult()`. סמל מטבע: `curSym()`. ערי עצירה: `stopsCitiesHtml()` (תוויות הלוך/חזור) + `placeName/resolvePlace`. טמפ׳ נחיתה: `landTempHtml()`+`fetchTemp/arrivalAbs`.
- **שרת — חילוץ עצירות:** `parseRoute()`/`stopsFromRoute()` (מתוך `t=` ב-link, מאומת מול `transfers`). `mapFare` שולח `stopsTo/stopsBack/returnTransfers/departAt/durationTo`.
- **ייבוא Google Flights:** לקוח `importGFlights()`+`pickCountryByIatas()`, תיבה `#gfUrl`/`#gfBtn`, טקסטים `T[lang].gf*`. שרת `parseGFlights()`/`extractLocations()`/`resolveMids()`/`fetchJSONUA()`/`b64urlToBuf()` → נקודת קצה `/parse-gflights`.
- **מדריך/עזרה:** `renderHelp()` בונה מ-`T[lang].help`; `#helpModal`+`openHelp/closeHelp/toggleHelp`, כפתור `#helpBtn`.
- **מפה:** `initMap/geocodeIata/arcPoints/bearing/focusMap`. **לוח/חודשים:** `showCalendar/fetchFlexMonths/fetchCheapestMonths`.
- **מדינות/דגלים:** `COUNTRIES` (code/iata/he/en/ru), `isoOf(c)` (קוד דגל), autocomplete יעד = `renderDrop`.
- **שמירה:** `saveState()/restoreState()` (localStorage `hipusState`) — נקרא בכל שינוי, משוחזר באתחול.

## סשן 2026-06-30 — מה נעשה ומה פתוח (המשך למחר)
**הכל נדחף ל-main ועלה ל-Render. מקומי = remote = ענן.** commits היום (מהישן לחדש):
1. `ce6d0e1` **ייבוא מקישור Google Flights/Explore** — נקודת קצה `/parse-gflights` מפענחת `tfs`/`tfu` (protobuf base64url) → תאריכים + מזהי Google (`/m/...`) → תרגום ל-IATA דרך **Wikidata** (P646→P238/P931). תיבת הדבקה בכרטיס החיפוש (`importGFlights`).
2. `1484f28` **תיקון אמינות מפה** — `geocodeIata` קיבל 3 ניסיונות חוזרים (autocomplete נכשל בקריאה קרה → המפה לא זזה).
3. `389c35e` **הסתרת תיבת הייבוא** מאחורי קוד סודי (`Ctrl+Shift+G` או `#gf`).
4. `57eee69` **≥10/12 טיסות לתקופה** — `fetchOffers` צובר exact+month (במקום return מוקדם).
5. `062bca3`+`9906de4` **חיפוש ברדיוס** — שדות סמוכים ליעד (עד **1500 ק"מ**, אותה מדינה, שדה ראשי של עיר `code===city_code`). שרת: `loadAirports`(נתוני TP, cache 24ש), `haversineKm`, `nearbyDests`, `fetchOffersLite`, `fetchOffersRadius`. הלקוח שולח `radius=1500`; כל הצעה מתויגת `dest/destKm/destCity`; הכרטיס מציג "~N ק"מ מהיעד".
6. `9906de4` **כפתור מלונות** בכל כרטיס (Hotellook, עיר נחיתה + תאריכים, marker 742171) + תווית **"הלוך ושוב"** מתחת למחיר.
7. `9b5bfdd` **נפילה לכיוון-אחד** — מסלולים כמו TLV→BOM אין להם הלוך-חזור ב-cache של TP אבל **יש כיוון-אחד**. `tpUrlOW` + fallback ב-`fetchOffers`/`fetchOffersLite`. הצעות מסומנות `oneWay`, הכרטיס מציג "כיוון אחד" בלי זמן חזרה.
8. `b79ecd7` **כפתור ייבוא בנאבבר** (`#gfToggleBtn`, אייקון קישור) + הוספת **Expedia/Issta/Israir** לקישורי ההשוואה.
9. `079e84a`+`c1db8c9` **עוד אתרי הזמנה בכל כרטיס** ("חיפוש גם ב:" — Google/Skyscanner/Kayak/Expedia, `offerBookLinks`), מעבירים נוסעים+מחלקה. כפתורי `.book-btn`/`.hotel-btn` עם listener על document (עוצר ניווט של הכרטיס).
10. `50e8ac5` **תיקון קישורי Google Flights** (ראה למטה — חשוב).

### ⚠️ למידה קריטית — קישורי Google Flights (אומת חי בדפדפן, לא לשכוח!)
- **tfs-protobuf שבניתי נדחה ע"י גוגל** (פתח דף בית). אל תחזור לזה בלי לאמת בדפדפן אמיתי.
- **הפורמט שעובד:** `https://www.google.com/travel/flights?q=Flights from {O} to {D} on {depISO} through {retISO}` (כיוון-אחד: בלי `through {ret}`).
- **נוסעים עובדים** בטקסט: ` {N} adults {M} children {K} infants`. **מחלקה ("business class") שוברת** את הפענוח → לא נכלל.
- **חובה `hl=en`** (+`gl=IL`): השאילתה באנגלית נפרסת **רק בלוקאל אנגלי**. `hl=iw`/`ru` → גוגל פותח דף בית (זה היה הבאג שהמשתמש ראה!). הפונקציה `googleFlightsUrl` כופה `hl=en`.
- אומת ב-Chrome: `q=...2 adults 1 child&hl=en&gl=IL&curr=ILS` → "12 results, for 3 passengers, ₪". עם `hl=iw` אותו URL → דף בית.

### פתוח למחר / רעיונות
- **מחלקה ל-Google**: לא נכנסת ב-`q=` (שוברת). הדרך היחידה = tfs **נכון**. כדי לבנות tfs נכון: לבצע חיפוש אמיתי ב-Google Flights דרך הטופס (עם Chrome MCP) ולתפוס את ה-URL שגוגל מייצר (`?tfs=...`), ואז לשחזר את מבנה הבייטים המדויק. (ה-tfs שלי היה מבוסס ניחוש שדות → נדחה.)
- **מקור נתונים חי (Amadeus Self-Service, חינמי)** — הפתרון האמיתי ל"מסך ריק" במסלולים דלים (TLV→BOM וכו'). דורש שהמשתמש יפתח מפתח חינמי ב-developers.amadeus.com ויספק API Key+Secret. השרת מוכן להוספת מקור (לאחד עם `fetchOffers`). המשתמש טרם אישר.
- **כיוון-אחד ב-Google**: כרגע תאריך יחיד → גוגל מציג הלוך-חזור עם חזרה אוטומטית. מקובל; אם רוצים true one-way צריך ניסוח/בדיקה.

## עקרונות עבודה (חשוב!)
- **גיבוי לפני כל שינוי** ל-`Backup/` (חותמת זמן: `index_YYYY-MM-DD_HHMMSS.html`).
- מבצעים שינוי **רק אחרי שהמשתמש כותב "תתחיל"/"תבצע"**.
- **יושר מעל הכול:** לא להמציא נתונים. אין מחיר אל על במקור → קישור בלבד; מחיר ממקור אחד (Aviasales) → לא נקרא "הזול ביותר".

## איך ממשיכים לפתח
1. הרץ מקומית (`הפעל.bat`) או ב-preview: `node server.js` (פורט 8787).
2. דחיפה לענן: `git add -A` → `git commit -m "..."` → `git push origin main`. Render בונה אוטומטית (~2 דק׳).
3. אימות ענן: `/health`, ו-`/offers?origin=TLV&destination=ATH&depart=2026-08-01&return=2026-08-08`.

## הערות סביבה / מלכודות (ממחשב זה)
- **`gh` (GitHub CLI) לא מותקן** כאן. PR-ים נוצרו דרך GitHub API עם ה-credential השמור (`git credential fill`). אם תתקין `gh` במחשב החדש — קל יותר.
- **זהות git** הוגדרה לוקלית: `Igal Karasik Orel <igal58@gmail.com>` (אם אין — `git config user.name/user.email`).
- **בדיקת ערים מול ה-API (חשוב — הדרך הנכונה)**: autocomplete **לא מספיק** ויכול להטעות. הבדיקה האמיתית = קריאה ל-`prices_for_dates` עם `departure_at=YYYY-MM` (חודש) ולבדוק את שדה `error`: `unknown location code` = קוד שבור; `airport X: not flightable` = נמל מבוטל; `success:true` עם `data` ריק = **תקין** (אין נתון לתאריך, לא באג). ככה נמצאו FRU ו-PNH שהביקורת הקודמת (autocomplete בלבד) פספסה.
- **מלכודת ב-preview (Claude Preview)**: כלי הצילום (`preview_screenshot`) נוטה להיתקע, ו-`getComputedStyle` מחזיר ערכים **תקועים** לתכונות מונפשות (color/opacity/visibility/transform) על אלמנטים שקיימים מהטעינה (לא משקף שינוי class). גם `window.innerWidth` דיווח 3px. לכן: אמת לוגיקה דרך `classList`/`textContent`/קריאות API (אמין), ולא דרך מדידות פריסה/צבע. אימות ויזואלי אמיתי — בדפדפן של המשתמש.
- **שרת robust**: `getJSON` עושה timeout 12ש + 3 ניסיונות (תיקן את ה"צריך כמה F5"). אל תחזיר את ההתנהגות הישנה.

## רעיונות שנשארו (לא נבנו עדיין)
- התראות מחיר: ה-MVP בנוי (מייל/Resend). נותר: להקים בענן (Resend key + CRON_KEY ב-Render+GitHub) ולעבור לאחסון מתמיד (Upstash) — ראה "הקמת התראות מחיר".
- פריסת דסקטופ split: מפה לצד התוצאות (כרגע המפה מתחת לחיפוש בכל הרזולוציות).
- deep-link מדויק לאל על עם מסלול/תאריכים — חסום (אין API ציבורי); רק אם המשתמש יספק URL אמיתי.
- הרחבת `AIRLINE_NAMES` (כרגע 55) לפי קודים חדשים שמופיעים.
- אופציונלי: פיצול ה-HTML לקבצים/קומפוננטות (כרגע קובץ יחיד בכוונה — אין build).

## סטטוס נוכחי (2026-06-30)
הכל עובד ובענן (`ce6d0e1`). מקומי = remote. אומת בסשן זה: ייבוא מקישור Google Flights/Explore — הקישור לדוגמה (TLV↔הודו, ממוקד מומבאי, 01–05/12/2026) נפתר ל-**TLV→BOM** עם תאריכים ולילות נכונים, גם מקומית וגם **בענן החי** (`/parse-gflights`), אפס warnings, אפס שגיאות console. ⚠️ למסלול הקצר הזה ל-Travelpayouts אין כרגע נאריף ב-cache → הכותרת+קישורי ההשוואה מוצגים אך ייתכן 0 כרטיסי מחיר (מגבלת מקור, לא באג).
⚠️ **מלכודת חוזרת:** `preview_screenshot` נתקע — אמת דרך `preview_eval` על ה-DOM (`classList`/`textContent`/קריאות API), שזה אמין; אימות ויזואלי אמיתי בדפדפן של המשתמש.

## תלות חיצונית חדשה
- **Open-Meteo** (`api.open-meteo.com` + `archive-api.open-meteo.com`) — מזג אוויר ביעד. חינמי, **ללא מפתח**, CORS, נקרא ישירות מהדפדפן (2026-06-25). אם ייפול → הטמפ׳ פשוט לא תוצג (הדף ממשיך לעבוד).
- **Wikidata SPARQL** (`query.wikidata.org/sparql`) — תרגום מזהי Google (`/m/...`) ל-IATA לצורך ייבוא Google Flights. חינמי, ללא מפתח, **נקרא מהשרת** (דורש User-Agent → `fetchJSONUA`) (2026-06-30). אם ייפול → המיקום מסומן "לא נפתר" וההודעה למשתמש (הדף ממשיך לעבוד).
